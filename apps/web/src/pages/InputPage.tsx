// Phase 5 — will be fully implemented with manual form + Excel import wired to /api/projects/import
export default function InputPage() {
  return (
    <div style={{ display:'flex', flex:1, alignItems:'center', justifyContent:'center', flexDirection:'column', gap:8, color:'#4B5563' }}>
      <span style={{ fontSize:32 }}>📝</span>
      <span style={{ fontSize:14, fontWeight:600 }}>Input Data Proyek</span>
      <span style={{ fontSize:12 }}>Diimplementasi pada Phase 5</span>
    </div>
  );
}
